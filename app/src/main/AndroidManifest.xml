<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.scepticallistic.kevin.tipscalc" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.scepticallistic.kevin.tipscalc.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.scepticallistic.kevin.tipscalc.AboutActivity"
            android:label="@string/action_about"
            android:screenOrientation="portrait"
            android:parentActivityName="com.scepticallistic.kevin.tipscalc.MainActivity" />
    </application>

</manifest>
